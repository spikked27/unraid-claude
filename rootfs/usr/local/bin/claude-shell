#!/usr/bin/env bash
# Open a normal interactive Claude session in the workspace (as the claude user).
if [ "$(id -u)" = "0" ]; then
    exec gosu claude env HOME=/config PATH="/config/.local/bin:$PATH" "$0" "$@"
fi
cd /config/workspace && exec claude "$@"
